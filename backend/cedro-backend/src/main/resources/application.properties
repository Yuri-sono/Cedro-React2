# suppress inspection "SpringBootApplicationProperties" for whole file
spring.application.name=cedro-backend

# Database
spring.datasource.url=jdbc:sqlserver://Cedro_banco.mssql.somee.com;databaseName=Cedro_banco;encrypt=true;trustServerCertificate=true
spring.datasource.username=Exodia_SQLLogin_1
spring.datasource.password=9s16ahvvlw
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=cedro_jwt_secret_key_2024_production_secure_min_32_chars
jwt.expiration=86400000

# Server
server.port=${PORT:3001}

# Logging
logging.level.com.cedro=DEBUG
logging.level.org.springframework.security=DEBUG

# CORS
cors.allowed-origins=http://localhost:3000,https://cedro-eight.vercel.app/

# Google OAuth
google.client.id=${GOOGLE_CLIENT_ID}
google.client.secret=${GOOGLE_CLIENT_SECRET}
